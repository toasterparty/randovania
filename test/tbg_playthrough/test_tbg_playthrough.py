import pytest

# Test that CommandType is contiguous
# Test that all mandatory Command methods are implemented for every CommandType
# Test that the above return the correct CommandType
# Test a playthrough of a set seed using a send_message that sends from a known buffer
